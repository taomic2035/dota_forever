# 交接 → GPT(视觉轨):2026-06-20 实玩暴露的渲染缺口

来源:用户实玩反馈。Opus 已实证核对——**机制都在 sim 里、数据正确,缺的是「看得见」**。请 GPT 补渲染。

---

## ⚠️ QA 阻断(2026-06-20):你的 map.ts 高地/森林改动**破坏平衡**,提交前必读

你在 `map.ts`/`mapLayout.ts` 加了**高地平台(`height[i]=2`)+ 森林口袋 + 斜坡**——方向**完全正确**(直接回应用户"高地约等于没有"),但 Opus 在整合树上 batchsim 实测:**8/8 → 6/8**(seed 31337/8088 跑满 90min 平局,净值 82k/72k)。

**根因**:高地增多 → 仰攻 25% miss + 高度遮断视野(看不见高台守军)→ **高地防守变"堡垒",bot 攻不上去** → 推不掉 → 僵局。seed 31337 打了 **204 杀仍平局**,证明不是没团战、是高地推不动。

**这是 sim 平衡红线**(本 sim:任何降低"击杀→目标"转化率的改动都会让 bot 收不掉尾)。**请勿直接提交当前 map 改动**。建议:
1. **收敛高地量**:减少平台数量/半径,**尤其关键目标(塔/兵营/高台基地)周围**别加太多高地——否则 bot 永远破不了高地防。
2. 高地可多放在**野区/边路**(视觉丰富 + 视野博弈),少放在**进攻必经的目标轴线**上。
3. 调好后 **ping Opus 重新 batchsim 验 8/8**(我把平衡关,你把视觉)。这是分工协作点,不是各做各的。

(Opus 侧也可配合:若你坚持高地量,我可评估微调 `UPHILL_MISS_CHANCE`/视野遮断,但那会削弱高地的意义,非首选。)

### 更新(2026-06-20 二次 QA):收敛高地**不够**
你已把高地收到 2 个野区瞭望丘(upper-jungle / hard-camp,r=320)——方向对,但 Opus 重 batchsim **仍 6/8**(平局种子从 31337+8088 变成 11+8088,数量没降)。
→ 说明**不是高地单独问题,是整套 map 改动(森林口袋 + 斜坡 + carving + 高地)合起来**把平衡从 8/8 扰到 ~6/8(森林/拓扑改变了 bot 的推进/绕路/视野)。
→ **请把 `map.ts`/`mapLayout.ts` 视为平衡敏感文件**:任何改地形拓扑(树/高地/可走区)的改动,提交前都要 ping Opus 跑 batchsim 验 8/8。当前这版**别提交**。
→ 根因是 bot 处理不了更丰富的地形(与 B2 树视野、A3 前摇同一红线)。彻底解需 bot 收尾/绕路/攻高地 AI(大专项,待用户立项)。在此之前,map 拓扑改动需克制到 batchsim 8/8。

---

## 实证数据(sim 侧确认存在,勿重复实现机制)

跑 `createWorld({creeps:true})` 快进 60s 实测:
- **河道神符**:`world.runes` = 2 个(`invis@(3600,3600)` 上河、`haste@(11440,11440)` 下河),神符点 2 个。**sim 正常生成**。
- **野怪**:30 只存活、10 个营地、Boss×1(嚎风狼等在野区)。**sim 正常生成**。
- **高地**:高度 3 档,高地 tile = 6972(占地面 ~13%);仰攻 25% miss + 高度遮断视野**都在 combat/vision**。
- **树木**:20552 tile。

## 请 GPT 补的渲染(按用户痛感排序)

### 高 —— 河道神符 3D 完全没画
`src/render3d/renderer3d.ts` **没有任何神符渲染代码**(grep `rune`=空)。2D 仅有「符文点淡紫地标」(`renderer.ts:164`),无生成后的神符本体高亮。
→ 用户在 3D 模式下**完全看不到神符**。请在 3D 加 `world.runes` 的可见标记(发光符文柱/图标,贴地、夜间更亮),2D 也建议把生成的神符画得更醒目(非仅点位)。

### 高 —— 高地视觉不分明(“高地约等于没有”)
高地机制都在(仰攻 miss + 看不见高台),但用户**看不出哪里是高地**。请在 2D/3D 用明确的崖壁/边缘高光/分层着色表达 `map.height`(0/1/2 三档),让玩家一眼识别低地↔高台,理解视野/miss 优势从何而来。

### 中 —— 树木太粗糙
用户原话「树木还是太粗糙了」。请精修树木模型/art + 强化树林「密林/阴影」观感(树冠遮蔽、林内压暗)。
注:**树林 LOS 隐蔽机制(B2)我试过即回滚**——减视野会让 bot 收不掉尾(batchsim 8/8→5/8 平局),需 bot 侦察 AI 配套,留作专项;但树木视觉精修独立于此、可先做。

### 中 —— 野怪/神符「找得到」
野怪在野区、被战争迷雾盖着,神符在河道——用户「没见到」。机制正确(无视野看不到),但建议:小地图营地/神符点指示、或首次入视野时高亮,降低「以为没有」的认知负担。

## 我(Opus)sim 侧已做
- 助攻金切出式 + 幻象金 + Roshan 奶酪:已提交推送(8/8)。
- 树林 LOS(B2):试过即回滚(破坏 bot 平衡,需侦察 AI 配套),留作专项。

分工不变:**机制/sim 归我,视觉/渲染归你**。共享文件按区块各改。
