# 核心机制保真度梳理(对照经典 DotA 1)

日期: 2026-06-12 · 方法: 5 簇并行只读审计(战斗抗性/死亡复活/经济时间地图/技能基元/物品血蓝) + 交叉核验
范围: **玩法规则正确性与完整性**(不含内容数量、UX/渲染)。配套:[架构与代码审计](2026-06-12-architecture-and-audit.md)

> 结论速览:机制**骨架完整、数值系数(19 血/力·13 蓝/智·1/7 甲/敏·0.06 护甲公式·25%/85% 魔抗)与 DotA 1 精确对齐**,
> 测试覆盖良好。缺口集中在**少数高价值规则保真点**,而非框架层。下面按优先级排,P0 为真正影响对局正确性的硬缺口。

---

## P0 — 高优先级(影响对局正确性的真实缺口)

### M1. ✅ 已完成 — 魔免 / 无敌 拦截「敌方负面 modifier 的施加」
- 现状:`combat.applyDamage` 正确在魔免下挡魔法伤害、无敌下挡一切伤害(已测)。但**控制/减益的施加路径不设防**——
  - 单位目标技能的 `onCast` 直接 `applyModifier`,不查 `magicImmune`/`invulnerable`;
  - AoE 的 `modifierArea` 查了 enemy 的 `magicImmune`(✓)但**没查 `invulnerable`**;
  - `sim/targeting.targetMatchesFilter` 不含 magicImmune/invulnerable 过滤。
- 差距:BKB/无敌单位**仍会被单体眩晕/减速命中**;无敌单位仍能被 AoE 写入 debuff。这是当前最严重的机制错误。
- 方案:在 `applyModifier`(或统一的 `canReceiveDebuff(target, mod)`)处,对**敌方来源的非 buff modifier**统一判定 `magicImmune`(可加 `piercesSpellImmunity` 例外位)与 `invulnerable`,一处收口,人类/AI/物品/技能一视同仁。体量 medium。

### M2. ✅ 已完成 — 必中(true strike) + 多来源闪避独立叠加
- 现状:`StatMods` 无 `trueStrike`;MKB 只做了重击眩晕,**不无视闪避**(MKB 的立身之本);多件闪避用 `Math.max` 聚合,而非独立概率乘算。
- 差距:闪避装互相不叠(35%+20% 实算成 35% 而非 48%);MKB 无法克制蝴蝶。
- 方案:加 `trueStrike` 位 + 闪避改独立乘算折叠;命中判定先查 trueStrike。体量 small。

### M3. ✅ 已完成 — 买活冷却(BUYBACK_COOLDOWN 300s + canBuyback)
- 现状:`hero.tryBuyback` 扣金即复活,**无任何冷却/惩罚字段**,可无限买活;定价仅按等级(`100+level²·1.5`),不含净资产。
- 差距:破坏后期买活博弈核心(DotA 1 买活有冷却且代价随净资产增长)。
- 方案:加 `heroMeta.buybackCooldownUntil`;买活后置冷却(经典约 ≥ 当前局剩余复活惩罚 / 固定数百秒);定价可叠加净资产项。体量 small。

### M4. ✅ 已完成 — 林肯法球法术格挡(tryLinkenBlock,13s)
- 现状:`item_linken_aura` 仅作属性标记,注释「格挡逻辑在 spell 施放处检查」**但施法路径无任何检查**。
- 差距:持林肯**毫无法术格挡效果**,核心反手装失效。
- 方案:单体敌对指向技/物命中前,检查目标 linken 标记 → 格挡一次并进 ~13s 冷却(标记上记 `blockReadyAt`)。与 M1 同在「目标可否受击」收口处实现最自然。体量 small–medium。

### M5. ✅ 已完成 — 符文双点同刷 + 幻象符
- 现状:`runes` 始终 ≤1,每周期只在一个点随机刷;种类 4 种(加速/双倍/恢复/隐身),**缺幻象符**。
- 差距:DotA 1 两个河道符文点**同时各刷一个**,玩家可争两个;经典 5 种含幻象。
- 方案:每周期两点各刷一枚;加 `illusion` 符(拾取生成 2 个幻象)。体量 small。

---

## P1 — 中优先级(规则保真偏差)

### M6. 缺失的技能基元:Break / 强驱散 / 法术吸血 / 睡眠受伤唤醒(大部完成)
- ✅ **睡眠受伤唤醒**:噩梦(班恩 E)已加 onTick 受伤唤醒。
- ✅ **强驱散(strong dispel)**:`purge(w,u,removeBuffs,strong)` 分级——基础驱散跳过硬控(眩晕/缠绕),强驱散全清;BKB/旋风/幻影斧=强,净魂/莲花/卫士胫甲/散华/永碟=基础。测试 strongDispel.test.ts(14)。
- ⬜ **Break(禁用被动)**:框架性,**当前无 break 物品/技能使用方**,待有需求再补。体量 medium。
- ⬜ **法术吸血(spell lifesteal)**:`lifesteal` 只挂普攻;法术伤害无吸血回路。框架性,当前无使用方。体量 small。

### M7. ✅ 已完成 — 承伤减免多来源独立乘算
- 已改 `1-∏(1-r_i)`(modifier 与物品两处 fold 一致);bash(MKB/基础/深渊)标记穿魔免(物理重击)。

### M8. 阿哈利姆神杖升级框架缺失
- `scepter` 只给属性,`AbilityDef` 无 `aghs` 变体钩子 → 无法升级大招。属框架层缺口(逐英雄变体是后续内容量)。体量 medium(框架)。

### M9. ✅ 已完成 — 可靠 / 不可靠金分账
- `heroMeta.reliableGold`;工资/击杀/拆塔/Boss 计可靠金,正补/野怪为不可靠。
- `loseGoldOnDeath` 仅扣不可靠金;`spendGold` 消费收口并回钳 reliableGold。测试 reliableGold.test.ts。

### M10. ✅ 已完成 — 经验/赏金数值校准至经典 DotA 1
- XP 升级表 `200+100·(n-1)`(原偏快的 55–65% 曲线已修正);英雄击杀经验 `100+36·lvl`(原 `+20` 偏低);经验共享圈/助攻圈 1300→1000;工资节奏 0.8s→0.6s(≈100 金/分);击杀赏金 `100+11·lvl`、连杀上限 500。详见 [balance-calibration](2026-06-12-balance-calibration.md)。
- 多种子平衡回归通过:seed 11/777/90210 均决胜,时长 43–54min(更贴近经典节奏,仍远在封顶内),击杀/正补合理。

### M11. ✅ 已完成 — 魔免/无敌单体施法预筛(物品)
- `useItem` 对敌方单体目标:无敌一律拒绝;魔免除非物品 `active.piercesSpellImmunity` 否则拒绝——**不扣蓝/不进 CD**(此前会「付代价但无效」)。
- `ActiveDef` 新增 `piercesSpellImmunity` 位;否决坠饰(反魔免专精)声明穿透,其沉默 modifier 亦带 `data.piercesSpellImmunity` 端到端落地。测试 itemImmunity.test.ts(4)。
- 仅改 `sim/items.ts` + `data/items.ts`,未触及 combat/modifiers 热点。

### M12. 零散规则点(部分完成)
- ✅ **幽灵/以太 +40% 魔法承伤**:physImmune 单位魔法伤害 ×1.4(combat 法术分支)。
- ✅ **chaos 攻击类型**:加入 AttackType + 矩阵(对所有护甲 100%,含要塞)。
- ✅ **重生(reincarnation)触发延迟**:致死触发 3s「重组」窗口(无敌+眩晕,不算被击杀),到期原地满血复活。测试 reincarnation.test.ts(8)。

---

## P2 — 低优先级 / 有意简化(多数留档即可,非必修)

- 死亡未清 `casting/channeling` 字段(无实际影响,字段不洁)。
- 暴击无伪随机分布(PRD)——手感差异,非错误。
- HP Removal 第四伤害类型缺失(当前英雄池无需求)。
- `noLifesteal` flag 声明未用(死代码,可清)。
- 平坦格挡以百分比减伤替代(先锋盾,设计取舍)。
- BKB 固定 6s,无递减(经典 9→6→5…)。
- 远古营地未限「仅英雄可清」;超级/精英兵赏金系数方向偏低;野区营地偏少。
- TP/远行可传至小兵旁(略宽松);闪烁超距缩短而非精确 1200。
- 反补己方低血建筑被禁(设计取舍);助攻不给金(**与早期 DotA 1 一致,非缺陷**;若对标 6.8x 后期再议)。
- 法球 unique 互斥未实现;放逐期间仍可被 AoE;变身无碰撞/外形改变;物品共享/传递未实现(单机)。
- Roshan(深渊领主)击杀经验只给击杀者(应附近全队)。

---

## 执行进度

**已完成 ✅**:
- **P0 全部**:魔免/无敌挡控制(M1)、必中+闪避独立叠加(M2)、买活冷却(M3)、林肯格挡(M4)、符文双点+幻象符(M5)。
- **M6** 睡眠受伤唤醒 + 强驱散分级;**M7** 承伤减免独立乘算;**M9** 可靠/不可靠金分账;
  **M10** 经验/赏金数值校准(多种子 batchsim 决胜回归通过);
  **M11** 魔免/无敌单体施法预筛(物品,省蓝/CD;否决坠饰穿透);
  **M12** 幽灵+40%魔法、chaos 攻击类型、**重生 3s 重组延迟**;
  **M8** 阿哈利姆神杖升级框架(数值轴 scepter.cooldown/manaCost/castRange + 行为轴 onCast 内 hasScepter 分支;示例:死神镰刀神杖降 CD 100→60、处决伤害 +40%)。

**剩余(均需更大投入或设计/平衡取舍):**
1. **M6 余下**:break(禁用被动)/ spell lifesteal——**框架性,当前无使用方**,待有需求物品/技能再补。
2. **逐英雄神杖变体**:M8 框架已就绪,各英雄具体神杖效果属内容量,随成品推进逐步补(遵「成品前不扩数量」)。
3. **P2**:多为有意简化,留档即可。

> 总体保真度:战斗抗性(魔免/无敌/必中/闪避/减免)、控制基元(强驱散/睡眠唤醒)、复活(买活冷却/重生重组)、
> 经济(可靠金/符文)、关键物品(林肯)的 DotA 1 手感均已补齐。余下为框架基元(无当前使用方)、
> 数值平衡校准(需回归)、神杖框架——价值/风险与「核心机制保真」递减。

---

## 第二轮机制审计(2026-06-12,三维并行对抗性复核)

对战斗管线 / 控制驱散 / 死亡经济三维做独立审计,揪出并修复了首轮遗漏的真实偏差:

**已修复 ✅**
- **A1 魔抗多源乘算**:此前 fold 纯加算(基础 25%+30% 抗装误得 45% 受伤),改为经典乘算 1-∏(1-r_i)(正确 47.5%)。核心抗性。batchsim 已复验:seed 11/777/90210 仍决胜(48–50min,击杀升至 57–58,法伤更致命=方向正确)。
- **A4 实例格挡分类**:折光(Refraction)经典只挡物理,此前误挡法术/纯粹 → 加 `blockPhysicalOnly` 标记;尖刺甲壳(Spiked Carapace)保持全类型(经典即反弹任意伤害)。
- **B1 被控禁用物品**:`useItem` 此前不查施法者状态,被眩晕/妖术单位仍能 BKB/TP 自救 → 新增 `muted` 态,门控 `stunned||muted`(沉默不禁物品,BKB 正用于反沉默);妖术(item_hex)设 muted。
- **B2 AoE 尊重穿魔免**:`modifierArea` 对魔免目标就地 `continue`,绕过 `piercesSpellImmunity` → 改为委托 `applyModifier`/`immuneToDebuff` 统一判定(穿透/无敌/buff/友方来源)。
- **A2/A3 chaos 攻击类型**:加 `isChaos` 守卫——chaos 穿以太(physImmune)且无视护甲减伤(矩阵已全 1.0);对现有非-chaos 攻击零影响。
- **MKB 免上坡 miss**:必中(trueStrike)此前仍吃 25% 低打高落空 → 改为必中同时无视闪避与上坡 miss(经典 MKB 行为)。
- **Nightmare 传递(③)**:Bane 噩梦受伤唤醒时,噩梦以剩余时长**传递给攻击者**(含己方误伤);remaining<0.5s 不再跳转,链条必然终止。
- **连杀阶梯对齐经典(③)**:终结连杀赏金改为阶梯 3→50/4→100/5→200/6→300/7→400/8+→500(原线性偏低)。
- **buyback 系数对齐经典(③)**:`100+lvl²×2`(原 ×1.5 偏低约 25%)。batchsim 复验决胜。

**留档不改(存疑或有意取舍)**
- **aegis 击杀赏金**(审计标 P0):审计称「持盾被杀不给击杀方赏金/经验」,但与 DotA 1/2 已知行为相悖(持盾被杀**计击杀且给赏金**,盾仅负责复活)。保持现状(给赏金),如需对标某版本再以权威机制核实。
- **冰甲减速穿魔免**:语义存疑(物理触发减速是否穿 BKB),无明确使用方收益,留待有需求再定。

---

## 第三轮审计(2026-06-13,5 维并行:物品 / 技能引擎 / 移动碰撞 / 视野迷雾 / 兵线野区塔)

**已修复 ✅**
- **塔真视(V2/B1,双代理确认)**:`TOWER_TRUE_SIGHT=900` 定义却从未挂载 → 隐身英雄塔下无敌。spawnBuildings 为每座塔施永久 trueSight modifier。
- **小兵仇恨(B2)**:`CREEP_AGGRO_RANGE=500` 定义却未用 → 英雄砍小兵不被群起追打。creepAggro 系统(置于 ordersSystem 之后)读 attack_launched 切火。
- **沉默中断引导(A1)**:progress() 引导分支加 silenced 检查(经典沉默打断 channel)。
- **技能侧无敌单体施法预筛(A3)**:startCast 对无敌敌方单体拒绝(不扣蓝/CD,M11 姊妹)。魔免侧因技而异(纯伤/穿透),留待逐技声明。
- **隐身物品攻击/施法即破(V3)**:影锋/银刃/烟雾/微光攻击或施法即解除隐身;**顺带修正** rune_invis 解除此前被「拾取节流 + 无符文早退」误挡的隐患(现每帧执行)。
- **Boss 魔抗 0.5→0.55**(经典 Roshan)。

**留档 / 后续(体量大或属内容量,遵「成品前不扩数量」)**
- **单位碰撞 / body-block**(移动审计新发现):collisionRadius 仅用于攻击距离判定,位移阶段单位可重叠,无卡位博弈。体量大(需碰撞分离系统),P2「变身无碰撞」已暗示有意简化方向。**【2026-06-13 用户决策:暂不实现,作为有意简化保留;核心机制视为收口。】**
- **技能侧魔免单体施法预筛**:魔免因技而异(纯粹伤害穿魔免、部分技穿透),需逐技 piercesSpellImmunity + 纯伤声明,属内容量;M1 已保证效果正确,仅省蓝/CD 体验优化。
- **物品夯实(2026-06-13)**:✅ 已修 — 秘法之靴 +250 法力、吸血战旗 +5 护甲光环、战争号角友方 +5 护甲/30% 攻速、Midas 给经验。⬜ 待框架 — 战争号角**敌方** -5 护甲(需 aura 支持多光环数组)、飓风之杖旋风对敌**无敌升空**(需 dispel-safe 的 modifier 授予 invulnerable 框架,直接置字段有早期驱散泄漏风险)、Satanic 吸血覆盖语义、Refresher 不刷自身(防无限刷,有意)。
- **低优先**:烟雾近敌方建筑不失效(V4)、兵线 leash 用垂距 1200(CREEP_LEASH_RANGE 600 未用)、phased 移动未消费、orb 未过滤 ward、幻象基于 base 而非 calc、野怪昼夜视野恒定、树木不阻挡视野(有意简化,影响插眼博弈)、兵营赏金(设计取舍)、若干 dead-code 常量。

> 三轮审计后,核心机制(战斗/抗性/控制/驱散/死亡复活/经济/视野真视/兵线仇恨/技能引擎/物品)对经典 DotA 1 的保真度已系统性收口;余下为大体量子系统(碰撞)、逐项内容量(逐技神杖/魔免、逐物品效果)与有意简化,均留档,不阻碍「首个成品」。
